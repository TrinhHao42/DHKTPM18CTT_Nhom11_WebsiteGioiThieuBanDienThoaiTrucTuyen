services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: phone_vectordb
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_DB: PhoneDB
    ports:
      - "5432:5432"
    networks:
      - app_network
    volumes:
      - postgres_data:/var/lib/postgresql/data
    command: >
      postgres
      -c shared_preload_libraries=vector

networks:
  app_network:
    driver: bridge

volumes:
  postgres_data: